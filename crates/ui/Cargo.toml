[package]
name = "gfm-ui"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
anyhow = "1.0"
gfm-types = { path = "../types" }
gpui = { version = "0.2.2", default-features = false, features = ["runtime_shaders"] }

[lints]
workspace = true
